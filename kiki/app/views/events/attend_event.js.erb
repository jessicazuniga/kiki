// $('#something').html('User: <%= @user_id %>  Event:  <%= @event_id %>  Response:  <%= @response %>')
$.mobile.changePage(window.location.href, {
    allowSamePageTransition: true,
    transition: 'none',
    reloadPage: true
});
