
<div data-role="page" id="eventlist">

	<div data-role="header">
		<h1>Kiki app</h1>
	</div>

	<div data-role="main" class="ui-content">

		<% event_time = @week_events.first.time%>
		
		<div data-role="collapsible" data-collapsed="false">
		<h4><%= Date::DAYNAMES[event_time.wday] %></h4>
		<ul data-role="listview">
		
			<% @week_events.each do |event| %>
		
				<% new_event_time = event.time%>
				<% if event_time.to_date != new_event_time.to_date %>
					<% event_time = new_event_time %>
					</ul>
					</div>
					<div data-role="collapsible" data-collapsed="true">
					<h4><%= Date::DAYNAMES[event_time.wday] %></h4>
					<ul data-role="listview">					
				<% end %>
		    	<li>
		    		<%= link_to event.name, event_path(event), :'data-transition' => 'slide' %>
		    	</li>
			<% end %>
		</ul>
		</div>

	</div>  

</div> 
